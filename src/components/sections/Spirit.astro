---
const { spirit } = Astro.locals.texts;
---

<style is:inline>
	section:is(.spirit) {
		padding-block: 100px !important;

		.quote-container {
			padding: 20px;
			background: rgb(13, 13, 13);
			border: 1px solid rgb(30, 30, 30);
			border-radius: 10px;
			max-width: 80ch;
			margin: 0 auto;
		}

		blockquote {
			font-size: 2em;
			letter-spacing: 0.04em;
			font-weight: 100;
			line-height: 1.8;
			margin-bottom: 30px;
		}
	}

	@media (width <= 600px) {
		section:is(.spirit) > div:is(.quote-container) > blockquote {
			font-size: 1.5em;
		}
	}
</style>

<section class="spirit">
	<div class="quote-container">
		<blockquote>
			{spirit.quoteText}
		</blockquote>
		<cite>— {spirit.quoteAuthor}</cite>
	</div>
</section>